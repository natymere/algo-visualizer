import { TGrid } from '../components/path-finding/grid.component';
import { TNode } from '../pages/pathfinding';

export const dijkstras = (grid: TGrid, startNode: TNode, endNode: TNode) => {
  let visited = [];
  let unvisited = [];
};
